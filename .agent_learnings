# Learnings from fixing Ollama installation failure in Dockerfile

- The Ollama installation script requires `zstd` for extraction.
- Even if a previous dependency (`wget`) was fixed, subsequent steps in the Dockerfile might reveal additional missing dependencies.
- Render build logs provide clear instructions on what is missing (e.g., "Please install zstd and try again").
